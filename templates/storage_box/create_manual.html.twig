{% extends 'base.html.twig' %}

{% block title %}Create Manual Storage Box - CS2 Inventory{% endblock %}

{% block body %}
<main class="w-full py-6 px-8">
    <!-- Flash Messages -->
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert-{{ label }} mb-4">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <!-- Page Header -->
    <div class="mb-8">
        <div class="flex items-center justify-between">
            <div>
                <h2 class="text-3xl font-bold text-white mb-2">Create Manual Storage Box</h2>
                <p class="text-gray-400">
                    Track items you've lent to friends
                </p>
            </div>
            <a href="{{ path('inventory_index') }}" class="btn-secondary">
                Back to Inventory
            </a>
        </div>
    </div>

    <!-- Info Card -->
    <div class="card mb-8 bg-gradient-to-br from-purple-900/30 to-purple-800/20 border-purple-500/50">
        <div class="flex items-start gap-4">
            <span class="text-4xl">🎁</span>
            <div class="flex-1">
                <h3 class="text-xl font-semibold text-white mb-2">What are Manual Storage Boxes?</h3>
                <p class="text-gray-300 leading-relaxed">
                    Manual storage boxes allow you to track CS2 items that you've lent to friends or stored outside your Steam inventory.
                    These boxes work identically to Steam-imported storage boxes but won't be affected by inventory imports.
                    You can deposit and withdraw items just like regular storage boxes.
                </p>
            </div>
        </div>
    </div>

    <!-- Create Form -->
    <div class="max-w-2xl mx-auto">
        <form action="{{ path('storage_box_create_manual_submit') }}" method="post">
            <div class="card">
                <label for="name" class="block text-lg font-medium text-white mb-2">
                    Storage Box Name <span class="text-red-400">*</span>
                </label>
                <input
                    type="text"
                    name="name"
                    id="name"
                    value="{{ name|default('') }}"
                    maxlength="255"
                    required
                    class="w-full px-4 py-3 bg-gray-900 border border-gray-700 rounded-lg text-gray-300 focus:outline-none focus:border-purple-500"
                    placeholder="e.g., Friend - John's Items"
                    autofocus
                >
                <p class="mt-2 text-sm text-gray-500">
                    Give your storage box a descriptive name (max 255 characters)
                </p>
            </div>

            <div class="alert-info mb-6">
                <strong>Tip:</strong> You can create multiple manual boxes to organize items by friend or category (e.g., "John - Knives", "Sarah - Rifles").
            </div>

            <div class="flex justify-end gap-4">
                <a href="{{ path('inventory_index') }}" class="btn-secondary">
                    Cancel
                </a>
                <button type="submit" class="btn-primary bg-purple-600 hover:bg-purple-700">
                    Create Storage Box
                </button>
            </div>
        </form>
    </div>
</main>
{% endblock %}
